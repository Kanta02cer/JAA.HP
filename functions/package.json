{
  "name": "jaa-hp-functions",
  "engines": {
    "node": "^20"
  },
  "type": "commonjs",
  "scripts": {
    "build": "echo noop",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "@sendgrid/mail": "^8.1.0",
    "cors": "^2.8.5"
  }
}

